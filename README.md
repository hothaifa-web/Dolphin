# Mock E-Store (Web)

Quick demo e-commerce single-repo app with role-based routing (Admin / Customer / Store / Driver), dark UI and Arabic/English support.

Getting started:

1. Install dependencies

```bash
cd "C:/Users/malomari-/Desktop/New folder (4)"
npm install
```

2. Run dev server

```bash
npm run dev
```

Demo credentials:
- Username: حذيفه  Password: 1  (Role: Admin)

Notes:
- This project uses Tailwind for styling and Chart.js for charts.
- All data is mock and located at `src/data/mock.js`.
